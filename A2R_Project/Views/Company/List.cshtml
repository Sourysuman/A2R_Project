@model A2R_Project.Models.CompanyViewModel

@{
    ViewBag.Title = "List";
}
<style>
    /* ================= GLOBAL ================= */
    body {
        background: #f4f7fb;
        font-family: 'Segoe UI', sans-serif;
        color: #000;
    }

    /* ================= CARD ================= */
    .card {
        border: none;
        border-radius: 16px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        background: #ffffff;
    }

    .card-title {
        font-size: 18px;
        font-weight: 600;
        letter-spacing: 0.5px;
        color: #111827;
    }

    /* ================= TABLE ================= */
    #companyTable {
        border-radius: 14px;
        overflow: hidden;
        background: #fff;
        border-collapse: collapse;
        border-spacing: 0;
        width: 100%;
    }

        #companyTable thead {
            background: #3f454c;
        }

            #companyTable thead th {
                color: #fff !important;
                border: none !important;
                font-size: 14px;
                font-weight: 700;
                text-transform: uppercase;
                letter-spacing: 0.6px;
                padding: 14px;
                text-align: center !important;
            }

        #companyTable tbody td {
            font-size: 15px;
            font-weight: 600;
            color: #34495e;
            padding: 14px;
            vertical-align: middle !important;
            text-align: center !important;
            background-color: white !important;
            border: none !important;
        }

            #companyTable tbody td:first-child {
                font-weight: 700 !important;
                color: #2c3e50 !important;
                font-size: 15px !important;
            }

        #companyTable tbody tr {
            transition: all 0.25s ease;
        }



        /* ===== TABLE ACTION BUTTONS ===== */
        #companyTable .btn-sm {
            width: 36px !important;
            height: 36px !important;
            border-radius: 10px !important;
            padding: 0 !important;
            margin: 0 !important;
            display: inline-flex !important;
            align-items: center !important;
            justify-content: center !important;
            border: none !important;
            transition: all 0.25s ease;
        }

            #companyTable .btn-sm i {
                font-size: 16px !important;
                line-height: 1;
            }

            #companyTable .btn-sm:hover {
                transform: translateY(-2px);
                box-shadow: 0 6px 15px rgba(0,0,0,0.15);
            }

        #companyTable .btn-gradient-primary {
            background: linear-gradient(135deg, #4e73df, #224abe) !important;
            color: #fff !important;
            border-radius: 10px !important;
        }

        #companyTable .btn-gradient-dark {
            background: linear-gradient(135deg, #36b9cc, #1f8ea3) !important;
            color: #fff !important;
            border-radius: 10px !important;
            border: none !important;
        }

        #companyTable .btn-gradient-warning {
            background: linear-gradient(135deg, #e74a3b, #c0392b) !important;
            color: #fff !important;
            border-radius: 10px !important;
            border: none !important;
        }

        #companyTable tbody td:last-child {
            text-align: center !important;
            vertical-align: middle !important;
            display: table-cell !important;
        }

            #companyTable tbody td:last-child .btn-group {
                display: inline-flex !important;
                align-items: center !important;
                justify-content: center !important;
                gap: 5px;
            }

    /* ================= BADGES ================= */
    .badge-success {
        background: #16a34a !important;
        color: #fff !important;
        padding: 5px 12px;
        border-radius: 20px;
        font-size: 13px;
        font-weight: 600;
    }

    .badge-danger {
        background: #dc2626 !important;
        color: #fff !important;
        padding: 5px 12px;
        border-radius: 20px;
        font-size: 13px;
        font-weight: 600;
    }

    /* ================= BUTTONS ================= */
    .btn {
        border-radius: 10px;
        font-size: 14px;
        font-weight: 600;
        padding: 6px 14px;
        transition: all 0.3s ease;
    }

        .btn:hover {
            transform: translateY(-2px) scale(1.05);
            box-shadow: 0 6px 15px rgba(0,0,0,0.2);
        }

    .btn-gradient-primary {
        background: linear-gradient(135deg, #10b981, #059669) !important;
        border: none !important;
        color: #fff !important;
        font-weight: 600;
        border-radius: 10px !important;
        padding: 8px 18px !important;
        transition: 0.2s;
    }

        .btn-gradient-primary:hover {
            transform: scale(1.05);
            box-shadow: 0 5px 15px rgba(16,185,129,0.4);
            color: #fff !important;
        }

    .btn-gradient-dark {
        background: linear-gradient(45deg, #434343, #000000);
        color: #fff;
        border: none;
    }

    .btn-gradient-warning {
        background: linear-gradient(45deg, #ff416c, #ff4b2b);
        color: #fff;
        border: none;
    }

    td .btn {
        margin: 3px;
    }

    /* ================= SHOW ALL BUTTON ================= */
    #showAllButton {
        background: linear-gradient(135deg, #10b981, #059669) !important;
        border: none !important;
        color: #fff !important;
        font-weight: 600;
        border-radius: 10px !important;
        padding: 8px 20px !important;
        transition: 0.2s;
    }

        #showAllButton:hover {
            transform: scale(1.05);
            box-shadow: 0 5px 15px rgba(16,185,129,0.4);
        }

    /* ================= PAGINATION ================= */
    .pagination .page-item .page-link {
        border-radius: 8px;
        margin: 0 4px;
        color: #4f46e5;
        font-weight: 500;
    }

    .pagination .page-item.active .page-link {
        background: linear-gradient(135deg, #6366f1, #4f46e5);
        color: #fff;
        border: none;
    }

    /* ================= FORM ELEMENTS ================= */
    .form-control {
        border-radius: 10px;
        border: 1px solid #ddd;
        padding: 10px;
        font-size: 14px;
        font-weight: 600;
        color: #000;
    }

        .form-control:focus {
            border-color: #4facfe;
            box-shadow: 0 0 8px rgba(79,172,254,0.5);
        }

    label {
        font-weight: 700;
        font-size: 13px;
        color: #000;
    }

    .mandatoryfield {
        color: red;
    }

    .validationMessage {
        color: red;
        font-size: 12px;
        font-weight: 600;
    }

    /* ================= IMAGE ================= */
    #logoPreview,
    #editLogoPreview,
    #image_upload_previewEmployeeView,
    #image_upload_previewCompanyDel {
        width: 100%;
        height: 140px;
        object-fit: cover;
        border-radius: 12px;
        border: 2px solid #e0e0e0;
        background: #fff;
        transition: 0.3s;
    }

    #logoPreview,
    #editLogoPreview {
        height: 130px;
        border: 2px dashed #00c6ff;
        padding: 4px;
    }

        #logoPreview:hover,
        #editLogoPreview:hover {
            border-color: #4facfe;
            transform: scale(1.05);
        }

    #image_upload_previewEmployeeView,
    #image_upload_previewCompanyDel {
        height: 180px;
    }

        #image_upload_previewEmployeeView:hover,
        #image_upload_previewCompanyDel:hover {
            transform: scale(1.04);
            border-color: #00c6ff;
            box-shadow: 0 10px 25px rgba(0,198,255,0.4);
        }

    /* ================= CHECKBOX ================= */
    .rounded-checkbox {
        transform: scale(1.2);
        margin-right: 5px;
    }

    /* ================= ADD & EDIT MODAL ================= */
    #addCompany .modal-content,
    #editCompany .modal-content {
        border-radius: 20px;
        border: none;
        box-shadow: 0 25px 70px rgba(0,0,0,0.35);
        overflow: hidden;
        animation: smoothPopup 0.35s ease;
    }

    @@keyframes smoothPopup {
        from {
            transform: translateY(-30px) scale(0.95);
            opacity: 0;
        }

        to {
            transform: translateY(0) scale(1);
            opacity: 1;
        }
    }

    #addCompany .modal-body,
    #editCompany .modal-body {
        background: #f8fbff;
        padding: 25px;
        max-height: unset !important;
        overflow: visible !important;
    }

    #addCompany .row,
    #editCompany .row {
        margin-bottom: 8px;
    }

    #addCompany .form-control,
    #editCompany .form-control {
        border-radius: 12px;
        border: 1px solid #ddd;
        font-weight: 600;
        transition: 0.3s;
    }

        #addCompany .form-control:focus,
        #editCompany .form-control:focus {
            border-color: #00c6ff;
            box-shadow: 0 0 10px rgba(0,198,255,0.4);
            transform: scale(1.02);
        }

    #addCompany .modal-footer,
    #editCompany .modal-footer {
        border-top: none;
        background: #fff;
    }

    #addCompany .btn-success {
        background: linear-gradient(45deg, #00c853, #64dd17);
        border: none;
    }

    #editCompany .btn-primary {
        background: linear-gradient(45deg, #0072ff, #00c6ff);
        border: none;
    }

    #addCompany .btn-warning,
    #editCompany .btn-warning {
        background: linear-gradient(45deg, #f7971e, #ffd200);
        border: none;
        color: #000;
    }

    #addCompany .btn:hover,
    #editCompany .btn:hover {
        transform: scale(1.05);
        box-shadow: 0 8px 20px rgba(0,0,0,0.25);
    }

    /* ================= DROPDOWN ================= */
    #addCompany select,
    #editCompany select {
        appearance: none;
        -webkit-appearance: none;
        -moz-appearance: none;
        background: #fff url("data:image/svg+xml;utf8,<svg fill='%230072ff' height='22' viewBox='0 0 24 24' width='22' xmlns='http://www.w3.org/2000/svg'><path d='M7 10l5 5 5-5z'/></svg>") no-repeat right 14px center;
        border-radius: 12px;
        border: 1px solid #ddd;
        padding: 12px 40px 12px 12px;
        font-size: 15px;
        font-weight: 600;
        line-height: 1.5;
        height: 45px;
        cursor: pointer;
    }

        #addCompany select:hover, #editCompany select:hover {
            border-color: #00c6ff;
        }

        #addCompany select:focus, #editCompany select:focus {
            border-color: #0072ff;
            box-shadow: 0 0 10px rgba(0,114,255,0.3);
        }

    .input-group-append {
        display: none !important;
    }

    /* ================= ISACTIVE SWITCH ================= */
    #addCompany .form-check,
    #editCompany .form-check {
        display: flex;
        align-items: center;
        gap: 10px;
    }

    #addCompany .form-check-input,
    #editCompany .form-check-input {
        appearance: none;
        width: 45px;
        height: 22px;
        background: #ccc;
        border-radius: 50px;
        position: relative;
        cursor: pointer;
        transition: 0.3s;
    }

        #addCompany .form-check-input::before,
        #editCompany .form-check-input::before {
            content: "";
            width: 18px;
            height: 18px;
            background: #fff;
            position: absolute;
            top: 2px;
            left: 3px;
            border-radius: 50%;
            transition: 0.3s;
        }

        #addCompany .form-check-input:checked,
        #editCompany .form-check-input:checked {
            background: linear-gradient(45deg, #00c853, #64dd17);
        }

            #addCompany .form-check-input:checked::before,
            #editCompany .form-check-input:checked::before {
                transform: translateX(22px);
            }

    #addCompany .form-check-label,
    #editCompany .form-check-label {
        font-weight: 600;
    }

    /* ================= FILE UPLOAD ================= */
    #addCompany .form-control-file,
    #editCompany .form-control-file {
        width: 100%;
        padding: 8px;
        border: 2px dashed #00c6ff;
        border-radius: 12px;
        background: #f8fbff;
        cursor: pointer;
        transition: 0.3s;
        font-weight: 600;
    }

        #addCompany .form-control-file:hover,
        #editCompany .form-control-file:hover {
            background: #e6f7ff;
            border-color: #0072ff;
            transform: scale(1.02);
        }

    #addCompany input[type="file"]::file-selector-button,
    #editCompany input[type="file"]::file-selector-button {
        border: none;
        padding: 8px 15px;
        border-radius: 8px;
        background: linear-gradient(45deg, #0072ff, #00c6ff);
        color: #fff;
        font-weight: 600;
        cursor: pointer;
        margin-right: 10px;
    }

        #addCompany input[type="file"]::file-selector-button:hover,
        #editCompany input[type="file"]::file-selector-button:hover {
            background: linear-gradient(45deg, #005bea, #00c6ff);
        }

    @@media (min-width: 576px) {
        .modal-dialog {
            max-width: 1000px;
            margin: 1.75rem auto;
        }
    }

    /* ================= VIEW & DELETE MODAL ================= */
    #viewCompany .modal-content,
    #deleteCompany .modal-content {
        border-radius: 20px;
        border: none;
        box-shadow: 0 25px 70px rgba(0,0,0,0.3);
        overflow: hidden;
        animation: popupSmooth 0.4s ease;
    }

    @@keyframes popupSmooth {
        from {
            transform: translateY(-25px) scale(0.95);
            opacity: 0;
        }

        to {
            transform: translateY(0) scale(1);
            opacity: 1;
        }
    }

    #viewCompany .modal-body,
    #deleteCompany .modal-body {
        padding: 25px;
        background: #ffffff;
    }

    #viewCompany .card,
    #deleteCompany .card {
        border-radius: 18px;
        border: none;
        background: #ffffff;
        box-shadow: 0 10px 25px rgba(0,0,0,0.15);
        padding: 10px;
    }

    /* ================= CLEAN FIELD ================= */
    .clean-field {
        display: flex;
        align-items: center;
        margin-bottom: 14px;
        padding: 6px 0;
        border-bottom: 1px solid #eaeaea;
    }

        .clean-field .label {
            min-width: 170px;
            font-weight: 700;
            color: #333;
            font-size: 14px;
        }

            .clean-field .label::after {
                content: " :";
                margin-left: 5px;
                color: #777;
            }

        .clean-field .value {
            margin-left: 15px;
            padding: 6px 12px;
            background: #ffffff;
            border-radius: 8px;
            border: 1px solid #e0e6f0;
            font-weight: 600;
            color: #000;
            min-width: 140px;
        }

        .clean-field:hover .value {
            background: #e6f0ff;
            transition: 0.3s;
        }

    /* ================= COMPANY NAME ================= */
    #lblvwCompanyName,
    #lblDelCompanyName {
        display: block;
        text-align: center;
        font-size: 26px;
        font-weight: 800;
        color: #0072ff;
        margin-bottom: 15px;
        letter-spacing: 1px;
    }

    /* ================= VIEW TITLE ================= */
    .view-section-title {
        text-align: center;
        font-size: 18px;
        font-weight: 700;
        color: #1e293b;
        position: relative;
        margin: 10px 0 20px;
        letter-spacing: 0.5px;
    }

        .view-section-title::before,
        .view-section-title::after {
            content: "";
            position: absolute;
            top: 50%;
            width: 30%;
            height: 2px;
            background: linear-gradient(45deg, #facc15, #f59e0b);
        }

        .view-section-title::before {
            left: 0;
        }

        .view-section-title::after {
            right: 0;
        }

    .view-subtitle {
        text-align: center;
        font-size: 13px;
        color: #64748b;
        margin-top: -10px;
        margin-bottom: 15px;
    }

    /* ================= VIEW/DELETE BUTTONS ================= */
    #viewCompany .btnClose,
    #deleteCompany .btn {
        border-radius: 25px;
        padding: 6px 18px;
        font-weight: 600;
    }


        #viewCompany .btn:hover,
        #deleteCompany .btn:hover {
            transform: scale(1.05);
            transition: 0.3s;
        }

    @@media (max-width: 768px) {
        #viewCompany .form-group,
        #deleteCompany .form-group {
            flex-direction: column;
            align-items: flex-start;
        }
    }

    /* ================= ALL MODAL HEADERS ================= */
    #addCompany .modal-header,
    #editCompany .modal-header,
    #viewCompany .modal-header,
    #deleteCompany .modal-header {
        background: linear-gradient(135deg, #2563eb, #1e40af) !important;
        color: #fff !important;
        border: none !important;
    }

    .modal-title {
        font-weight: 700;
        font-size: 20px;
    }
    /* ===== ALL MODAL HEADER TEXT WHITE ===== */
    #addCompany .modal-header .modal-title,
    #addCompany .modal-header .close,
    #editCompany .modal-header .modal-title,
    #editCompany .modal-header .close,
    #viewCompany .modal-header .modal-title,
    #viewCompany .modal-header .close,
    #deleteCompany .modal-header .modal-title,
    #deleteCompany .modal-header .close {
        color: #fff !important;
    }

    .modal-backdrop {
        opacity: 0.25 !important;
    }

    #addCompany .form-control, #editCompany .form-control {
        border-radius: 12px;
        border: 1px solid #ddd;
        font-weight: 300;
        transition: 0.3s;
    }
</style>
<div class="col-lg-12 stretch-card">
    <div class="card">
        <div class="card-body">
            <div class="d-flex justify-content-between align-items-center" style="padding: 10px !important">
                <strong class="card-title">Company List</strong>
                @if (ViewBag.CanInsert == true)
                {
                    <button type="button" class="btn btn-gradient-primary btn-rounded btn-fw"
                            data-toggle="modal" data-target="#addCompany">
                        <i class="fa fa-plus"></i> Add
                    </button>
                }
                else
                {
                    <button type="button" class="btn btn-secondary btn-rounded btn-fw"
                            onclick="noAccess('Insert')">
                        <i class="fa fa-plus"></i> Add
                    </button>
                }
            </div>
            <table class="table table-bordered" id="companyTable">
                <thead>
                    <tr>
                        <th>Company Name</th>
                        <th>Address</th>
                        <th>City</th>
                        <th>State</th>
                        <th>Country</th>
                        <th>Status</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.companies.Count() > 0)
                    {
                        foreach (var company in Model.companies)
                        {
                            <tr class="table-info">
                                <td class="table-primary">@company.CompanyName</td>
                                <td class="table-warning">@company.Address1</td>
                                <td class="table-primary">@company.City</td>
                                <td class="table-warning">@company.StateName</td>
                                <td class="table-primary">@company.CountryName</td>
                                <td class="table-warning">
                                    @if (company.IsActive == 1)
                                    {
                                        <span class="badge badge-success">Active</span>
                                    }
                                    else
                                    {
                                        <span class="badge badge-danger">Inactive</span>
                                    }
                                </td>
                                <td>
                                    @* EDIT *@
                                    @if (ViewBag.CanUpdate == true)
                                    {
                                        <button type="button" class="btn btn-sm btn-custom btn-gradient-primary btn-rounded editByCompanyId"
                                                data-company-id="@company.CompanyID"
                                                data-toggle="modal" data-target="#editCompany"
                                                title="Edit">
                                            <i class="mdi mdi-pencil"></i>
                                        </button>
                                    }
                                    else
                                    {
                                        <button type="button" class="btn btn-sm btn-secondary btn-rounded"
                                                title="No Edit Permission"
                                                onclick="noAccess('Update')">
                                            <i class="mdi mdi-pencil"></i>
                                        </button>
                                    }

                                    @* VIEW — always visible *@
                                    <button type="button" class="btn btn-sm btn-custom btn-gradient-dark btn-rounded viewByCompanyId"
                                            data-company-id="@company.CompanyID"
                                            data-toggle="modal" data-target="#viewCompany"
                                            title="View">
                                        <i class="mdi mdi-eye"></i>
                                    </button>

                                    @* DELETE *@
                                    @if (ViewBag.CanDelete == true)
                                    {
                                        <button type="button" class="btn btn-sm btn-custom btn-gradient-warning btn-rounded deleteCompanies"
                                                data-company-id="@company.CompanyID"
                                                data-toggle="modal" data-target="#deleteCompany"
                                                title="Delete">
                                            <i class="mdi mdi-delete"></i>
                                        </button>
                                    }
                                    else
                                    {
                                        <button type="button" class="btn btn-sm btn-secondary btn-rounded"
                                                title="No Delete Permission"
                                                onclick="noAccess('Delete')">
                                            <i class="mdi mdi-delete"></i>
                                        </button>
                                    }
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
            <div class="row mt-3">
                <div class="col-md-6">
                    <button id="showAllButton" class="btn btn-gradient-primary">
                        Show All
                    </button>
                </div>

                <div class="col-md-6 text-right">
                    <ul id="pagination" class="pagination justify-content-end"></ul>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Add Company Modal -->
<div class="modal fade" id="addCompany" tabindex="-1" role="dialog" aria-labelledby="addCompanyModalLabel" aria-hidden="true">

    <div class="modal-dialog modal-xl modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="addCompanyModalLabel">Add Company</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form id="addCompanyForm" class="form-sample">
                    <div class="row">
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtCompanyName">Company Name<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtCompanyName" name="txtCompanyName" required />
                            </div><span class="validationMessage" id="msgAddCompanyNameValidation"></span>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtCompanyAddress1">Company Address1<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtCompanyAddress1" name="txtCompanyAddress1" required />
                            </div><span class="validationMessage" id="msgAddress1Validation"></span>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtCompanyAddress2">Company Address2</label>
                                <input type="text" class="form-control" id="txtCompanyAddress2" name="txtCompanyAddress2" />
                            </div><span class="validationMessage" id="msgAddress2Validation"></span>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtArea">Area</label>
                                <input type="text" class="form-control" id="txtArea" name="txtArea" />
                            </div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtCity">City<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtCity" name="txtCity" required />
                            </div><span class="validationMessage" id="msgAddCityValidation"></span>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="ddlState">State<span class="mandatoryfield">*</span></label>
                                <div class="input-group">
                                    @Html.DropDownList("ddlStateId", new SelectList(Model.states, "StateID", "StateName"), "--Select State l--", new { @class = "form-control form-control-sm", @id = "ddlStateId" })
                                    <div class="input-group-append">
                                        <span class="input-group-text" style="height: 35px;"><i class="mdi mdi-menu-down"></i></span>
                                    </div><span class="validationMessage" id="msgAddStateValidation"></span>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="ddlCountry">Country<span class="mandatoryfield">*</span></label>
                                <div class="input-group">
                                    @Html.DropDownList("ddlCountryId", new SelectList(Model.country, "CountryID", "CountryName"), "--Select Country --", new { @class = "form-control form-control-sm", @id = "ddlCountryId" })
                                    <div class="input-group-append">
                                        <span class="input-group-text" style="height: 35px;"><i class="mdi mdi-menu-down"></i></span>
                                    </div><span class="validationMessage" id="msgAddCountryValidation"></span>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtPincode">Pin Code<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtPincode" name="txtPincode" required />
                            </div><span class="validationMessage" id="msgAddPinCodeValidation"></span>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtContactNo">Contact No.<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtContactNo" name="txtContactNo" />
                            </div><span class="validationMessage" id="msgAddContactNoValidation"></span>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtEmail">Email<span class="mandatoryfield">*</span></label>
                                <input type="email" class="form-control" id="txtEmail" name="txtEmail" />
                            </div><span class="validationMessage" id="msgAddEmailValidation"></span>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtPAN">PAN<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtPAN" name="txtPAN" />
                            </div><span class="validationMessage" id="msgAddPanValidation"></span>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtTAN">TAN<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtTAN" name="txtTAN" />
                            </div><span class="validationMessage" id="msgAddTanValidation"></span>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtVAT">VAT<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtVAT" name="txtVAT" />
                            </div><span class="validationMessage" id="msgAddVatValidation"></span>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtGST">GST<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtGST" name="txtGST" />
                            </div><span class="validationMessage" id="msgAddGstValidation"></span>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtTIN">TIN<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtTIN" name="txtTIN" />
                            </div><span class="validationMessage" id="msgAddTinValidation"></span>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtPT">PT<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtPT" name="txtPT" />
                            </div><span class="validationMessage" id="msgAddPtValidation"></span>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtSMTPServerName">SMTP Server Name<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtSMTPServerName" name="txtSMTPServerName" />
                            </div><span class="validationMessage" id="msgAddSmtpValidation"></span>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtSMTPPortNo">SMTP Port No.<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtSMTPPortNo" name="txtSMTPPortNo" />
                            </div><span class="validationMessage" id="msgAddSmtpPoNoValidation"></span>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtSMTPUserName">SMTP User Name<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtSMTPUserName" name="txtSMTPUserName" />
                            </div><span class="validationMessage" id="msgAddSmtpUserNameValidation"></span>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtSMTPPassword">SMTP Password<span class="mandatoryfield">*</span></label>
                                <input type="password" class="form-control" id="txtSMTPPassword" name="txtSMTPPassword" />
                            </div><span class="validationMessage" id="msgAddSmtpPassValidation"></span>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="logoUpload">Upload Logo (Choose File)</label>
                                <input type="file" class="form-control-file" id="logoUpload" name="logoUpload" accept="image/*" />
                            </div>
                        </div>
                        <div class="col-md-3">
                            <img id="logoPreview" src="~/Companys/Images/default.jpg" alt="Preview" style="max-width: 100px; max-height: 64px;" />
                        </div>
                        <div class="col-md-3">
                            <div class="form-check">
                                <input type="checkbox" class="form-check-input rounded-checkbox" id="chkActiveStatusAdd" checked>
                                <label class="form-check-label" for="chkActiveStatusAdd">IsActive</label>
                            </div>
                        </div>
                    </div>
                </form>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <button type="button" class="btn btn-warning reset-btn" style="cursor:pointer !important;">Reset</button>
                    <button type="button" class="btn btn-success add-submit" style="cursor:pointer !important;">Submit</button>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Edit Company Modal -->
<div class="modal fade" id="editCompany" tabindex="-1" role="dialog" aria-labelledby="editCompanyModalLabel" aria-hidden="true">

    <div class="modal-dialog modal-xl modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="editCompanyModalLabel">Edit Company</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form id="editCompanyForm" class="form-sample">
                    <div class="row">
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtEditCompanyName">Company Name<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtEditCompanyName" name="txtEditCompanyName" required />
                            </div><span class="validationMessage" id="msgEditCompanyNameValidation"></span>
                            <input type="hidden" id="hdnCompanyId" name="companyId">
                            <input type="hidden" id="hdnCompanyImageId" name="CompanyImagId">
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtEditCompanyAddress1">Company Address1<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtEditCompanyAddress1" name="txtEditCompanyAddress1" required />
                            </div><span class="validationMessage" id="msgEditAddress1Validation"></span>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtEditCompanyAddress2">Company Address2</label>
                                <input type="text" class="form-control" id="txtEditCompanyAddress2" name="txtEditCompanyAddress2" />
                            </div><span class="validationMessage" id="msgEditAddress2Validation"></span>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtEditArea">Area</label>
                                <input type="text" class="form-control" id="txtEditArea" name="txtEditArea" />
                            </div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtEditCity">City<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtEditCity" name="txtEditCity" required />
                            </div><span class="validationMessage" id="msgEditCityValidation"></span>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="ddlEditState">State<span class="mandatoryfield">*</span></label>
                                <div class="input-group">
                                    @Html.DropDownList("ddlEditStateId", new SelectList(Model.states, "StateID", "StateName"), "--Select State Name--", new { @class = "form-control form-control-sm", @id = "ddlEditStateId" })
                                    <div class="input-group-append">
                                        <span class="input-group-text" style="height: 35px;"><i class="mdi mdi-menu-down"></i></span>
                                    </div><span class="validationMessage" id="msgEditStateValidation"></span>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="ddlEditCountry">Country<span class="mandatoryfield">*</span></label>
                                <div class="input-group">
                                    @Html.DropDownList("ddlEditCountryId", new SelectList(Model.country, "CountryID", "CountryName"), "--Select Country Name--", new { @class = "form-control form-control-sm", @id = "ddlEditCountryId" })
                                    <div class="input-group-append">
                                        <span class="input-group-text" style="height: 35px;"><i class="mdi mdi-menu-down"></i></span>
                                    </div><span class="validationMessage" id="msgEditCountryValidation"></span>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtEditPincode">Pin Code<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtEditPincode" name="txtEditPincode" required />
                            </div><span class="validationMessage" id="msgEditPinCodeValidation"></span>
                        </div>
                    </div>
                    <!-- Continue with all other fields exactly like Add modal but with "Edit" prefix -->
                    <div class="row">
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtEditContactNo">Contact No.<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtEditContactNo" name="txtEditContactNo" />
                            </div><span class="validationMessage" id="msgEditContactNoValidation"></span>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtEditEmail">Email<span class="mandatoryfield">*</span></label>
                                <input type="email" class="form-control" id="txtEditEmail" name="txtEditEmail" />
                            </div><span class="validationMessage" id="msgEditEmailValidation"></span>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtEditPAN">PAN<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtEditPAN" name="txtEditPAN" />
                            </div><span class="validationMessage" id="msgEditPanValidation"></span>
                        </div>

                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtEditTAN">TAN<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtEditTAN" name="txtEditTAN" />
                            </div><span class="validationMessage" id="msgEditTanValidation"></span>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtEditVAT">VAT<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtEditVAT" name="txtEditVAT" />
                            </div><span class="validationMessage" id="msgEditVatValidation"></span>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtEditGST">GST<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtEditGST" name="txtEditGST" />
                            </div><span class="validationMessage" id="msgEditGstValidation"></span>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtEditTIN">TIN<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtEditTIN" name="txtEditTIN" />
                            </div><span class="validationMessage" id="msgEditTinValidation"></span>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtEditPT">PT<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtEditPT" name="txtEditPT" />
                            </div><span class="validationMessage" id="msgEditPtValidation"></span>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtEditSMTPServerName">SMTP Server Name<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtEditSMTPServerName" name="txtEditSMTPServerName" />
                            </div><span class="validationMessage" id="msgEditSmtpValidation"></span>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtEditSMTPPortNo">SMTP Port No.<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtEditSMTPPortNo" name="txtEditSMTPPortNo" />
                            </div><span class="validationMessage" id="msgEditSmtpPoNoValidation"></span>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtEditSMTPUserName">SMTP User Name<span class="mandatoryfield">*</span></label>
                                <input type="text" class="form-control" id="txtEditSMTPUserName" name="txtEditSMTPUserName" />
                            </div><span class="validationMessage" id="msgEditSmtpUserNameValidation"></span>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="txtEditSMTPPassword">SMTP Password<span class="mandatoryfield">*</span></label>
                                <input type="password" class="form-control" id="txtEditSMTPPassword" name="txtEditSMTPPassword" />
                            </div><span class="validationMessage" id="msgEditSmtpPassValidation"></span>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="editLogoUpload">Upload Logo (Choose File)</label>
                                <input type="file" class="form-control-file" id="editLogoUpload" name="editLogoUpload" accept="image/*" />
                            </div>
                        </div>
                        <div class="col-md-3">
                            <img id="editLogoPreview" src="~/Companys/Images/default.jpg" alt="Preview" style="max-width: 100px; max-height: 64px;" />
                        </div>
                        <div class="col-md-3">
                            <div class="form-check">
                                <input type="checkbox" class="form-check-input rounded-checkbox" id="chkEditActiveStatus">
                                <label class="form-check-label" for="chkEditActiveStatus">IsActive</label>
                            </div>
                        </div>
                    </div>
                </form>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <button type="button" class="btn btn-warning reset-btn" style="cursor:pointer !important;">Reset</button>
                    <button type="button" class="btn btn-primary edit-submit" style="cursor:pointer !important;">Submit</button>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- View Company Modal -->
<div class="modal fade" id="viewCompany" tabindex="-1" role="dialog" aria-labelledby="mediumModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="mediumModalLabel">View Company Details</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div class="row">
                    <div class="col-md-4">
                    </div>
                    <div class="col-md-8">
                        <div class="view-section-title">
                            Individual Company Details
                        </div>
                        <div class="view-subtitle">
                            Complete information about the selected company
                        </div>
                        <span id="lblvwCompanyName" style="color:black; font-size: larger !important"></span><br />
                        <input type="hidden" id="hdnViewCompanyId" name="companyID">
                        <input type="hidden" id="hdnViewCompanyImageId" name="imageID">
                        <br />
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-4">
                        <div class="card">
                            <div class="card-body">
                                <img id="image_upload_previewEmployeeView" class="card-img-top col-md-8" src="~/Companys/Images/default.jpg" alt="Company Logo" style="min-width:100%; min-height:100% !important">
                            </div>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="form-group clean-field">
                            <span class="label">Company Email</span>
                            <span class="value" id="lblvwCompanyEmail"></span>
                        </div>

                        <div class="form-group clean-field">
                            <span class="label">Company Address1</span>
                            <span class="value" id="lblvwCompanyAddress1"></span>
                        </div>

                        <div class="form-group clean-field">
                            <span class="label">Company City</span>
                            <span class="value" id="lblvwCompanyCity"></span>
                        </div>

                        <div class="form-group clean-field">
                            <span class="label">Company State</span>
                            <span class="value" id="lblvwCompanyState"></span>
                        </div>

                        <div class="form-group clean-field">
                            <span class="label">Company Country</span>
                            <span class="value" id="lblvwCompanyCountry"></span>
                        </div>
                    </div>
                    <div class="col-md-4">
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Delete Company Modal -->
<div class="modal fade" id="deleteCompany" tabindex="-1" role="dialog" aria-labelledby="mediumModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="mediumModalLabel" style="color:red">Confirm Delete Company</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div class="row">
                    <div class="col-md-4">
                        <div class="card">
                            <div class="card-body">
                                <img id="image_upload_previewCompanyDel" class="card-img-top col-md-8" src="~/Companys/Images/default.jpg" alt="Company Logo" style="min-width:100%; min-height:100% !important">
                                <input type="hidden" id="hdnDelCompanyId" name="companyId">
                            </div>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <span id="lblDelCompanyName" style="color:black; font-size: larger !important"></span> <br />
                        <br />
                        <div class="form-group clean-field">
                            <span class="label">Company Email</span>
                            <span class="value" id="lblDelCompanyEmail"></span>
                        </div>

                        <div class="form-group clean-field">
                            <span class="label">Company Address1</span>
                            <span class="value" id="lblDelCompanyAddress1"></span>
                        </div>

                        <div class="form-group clean-field">
                            <span class="label">Company City</span>
                            <span class="value" id="lblDelCompanyCity"></span>
                        </div>

                        <div class="form-group clean-field">
                            <span class="label">Company State</span>
                            <span class="value" id="lblDelCompanyState"></span>
                        </div>

                        <div class="form-group clean-field">
                            <span class="label">Company Country</span>
                            <span class="value" id="lblDelCompanyCountry"></span>
                        </div>
                    </div>
                    <div class="col-md-4">
                    </div>
                </div>
                <div class="modal-footer">
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                        <button type="button" class="btn btn-danger delete-submit">DELETE</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
    document.addEventListener('DOMContentLoaded', function() {
        console.clear();
           // Permission denied alert
    window.noAccess = function(type) {
        Swal.fire({
            icon: 'error',
            title: 'Access Denied',
            text: 'You do not have ' + type + ' permission for Company module.',
            confirmButtonColor: '#dc2626'
        });
    }
        // =================================================================
        // 🔥 IMAGE PREVIEW FUNCTIONS
        // =================================================================
        function setupImagePreview(inputId, previewId) {
            const input = document.getElementById(inputId);
            const preview = document.getElementById(previewId);
            input.addEventListener('change', function(e) {
                const file = e.target.files[0];
                if (file) {
                    const reader = new FileReader();
                    reader.onload = function(e) { preview.src = e.target.result; };
                    reader.readAsDataURL(file);
                } else {
                    preview.src = '/Companys/Images/default.jpg';
                }
            });
        }
        setupImagePreview('logoUpload', 'logoPreview');
        setupImagePreview('editLogoUpload', 'editLogoPreview');

        // =================================================================
        // 🔥 ADD COMPANY
        // =================================================================
        document.querySelectorAll('.add-submit').forEach(btn => {
            btn.onclick = async function(e) {
                e.preventDefault();
                const required = {
                    'txtCompanyName': 'Company Name',
                    'txtCompanyAddress1': 'Address 1',
                    'txtCity': 'City',
                    'ddlStateId': 'State',
                    'ddlCountryId': 'Country',
                    'txtPincode': 'Pincode'
                };
                for (let [id, label] of Object.entries(required)) {
                    const field = document.getElementById(id);
                    if (!field.value?.trim()) {
                        alert(`⚠️ ${label} is required!`);
                        field.focus();
                        return false;
                    }
                }
                const formData = new FormData();
                const addFile = document.getElementById('logoUpload').files[0];
                if (addFile) formData.append('file', addFile);

                const companyData = {
                    Comp_Name: document.getElementById('txtCompanyName').value,
                    Comp_Address1: document.getElementById('txtCompanyAddress1').value,
                    Comp_Address2: document.getElementById('txtCompanyAddress2').value || '',
                    Comp_Area: document.getElementById('txtArea').value || '',
                    Comp_City: document.getElementById('txtCity').value,
                    Comp_State_ID: parseInt(document.getElementById('ddlStateId').value),
                    Comp_Country_ID: parseInt(document.getElementById('ddlCountryId').value),
                    Comp_Pincode: document.getElementById('txtPincode').value,
                    Comp_PAN_No: document.getElementById('txtPAN').value || '',
                    Comp_TAN_No: document.getElementById('txtTAN').value || '',
                    Comp_TIN_No: document.getElementById('txtTIN').value || '',
                    Comp_VAT_No: document.getElementById('txtVAT').value || '',
                    Comp_GST_No: document.getElementById('txtGST').value || '',
                    Comp_PTTax_No: document.getElementById('txtPT').value || '',
                    Comp_Email: document.getElementById('txtEmail').value || '',
                    Comp_Contact_No: document.getElementById('txtContactNo').value || '',
                    SMTP_Server_Name: document.getElementById('txtSMTPServerName').value || '',
                    SMTP_Port_No: document.getElementById('txtSMTPPortNo').value || '',
                    SMTP_User_Name: document.getElementById('txtSMTPUserName').value || '',
                    SMTP_User_Password: document.getElementById('txtSMTPPassword').value || '',
                    IsActive: document.getElementById('chkActiveStatusAdd').checked ? 1 : 0
                };
                formData.append('companyJson', JSON.stringify(companyData));
                try {
                    const response = await fetch('@Url.Action("Save", "Company")', { method: 'POST', body: formData });
                    const result = await response.json();
                    if (result.success !== false) {
                        alert('✅ Company Added Successfully!');
                        $('#addCompany').modal('hide');
                        document.getElementById('addCompanyForm').reset();
                        document.getElementById('logoPreview').src = '/Companys/Images/default.jpg';
                        setTimeout(() => location.reload(), 1500);
                    } else {
                        alert('❌ ' + (result.message || 'Failed to add company'));
                    }
                } catch (error) {
                    alert('❌ Network Error - Please try again');
                }
            };
        });

        // =================================================================
        // 🔥 EDIT BUTTON - FIXED DATA LOADER (SP returns aliases)
        // =================================================================
        document.querySelectorAll('.editByCompanyId').forEach(btn => {
            btn.addEventListener('click', function() {
                const companyId = this.dataset.companyId;
                fetch(`@Url.Action("GetCompany", "Company")?companyID=${companyId}`)
                    .then(r => r.json())
                    .then(data => {
                        console.log("📥 EDIT DATA:", data);
                        document.getElementById('hdnCompanyId').value = data.CompanyID || companyId;
                        fillEditForm(data);

                            const editPreview = document.getElementById('editLogoPreview');
    const imagePath = data.picturePath;
    if (imagePath && imagePath.trim() !== '') {
        editPreview.src = imagePath;
        editPreview.onerror = () => { editPreview.src = '/Companys/Images/default.jpg'; };
    } else {
        editPreview.src = '/Companys/Images/default.jpg';
    }
                    })
                    .catch(err => {
                        console.error("❌ EDIT ERROR:", err);
                        alert('Failed to load company data');
                    });
            });
        });

        // =================================================================
        // 🔥 VIEW BUTTON - FIXED (SP aliases: CompanyName, Address1, Email, StateName, CountryName)
        // =================================================================
        document.querySelectorAll('.viewByCompanyId').forEach(btn => {
            btn.addEventListener('click', function() {
                const id = this.dataset.companyId;
                fetch(`@Url.Action("GetCompany", "Company")?companyID=${id}`)
                    .then(r => r.json())
                    .then(data => {
                        console.log("👁️ VIEW DATA:", data);

                        document.getElementById('lblvwCompanyName').textContent     = data.companyName || 'N/A';
    document.getElementById('lblvwCompanyEmail').textContent    = data.email       || 'N/A';
    document.getElementById('lblvwCompanyAddress1').textContent = data.address1    || 'N/A';
    document.getElementById('lblvwCompanyCity').textContent     = data.city        || 'N/A';
    document.getElementById('lblvwCompanyState').textContent    = data.stateName   || 'N/A';
    document.getElementById('lblvwCompanyCountry').textContent  = data.countryName || 'N/A';

                           const viewImg = document.getElementById('image_upload_previewEmployeeView');
    const imagePath = data.picturePath;
    if (imagePath && imagePath.trim() !== '') {
        viewImg.src = imagePath;
        viewImg.onerror = () => { viewImg.src = '/Companys/Images/default.jpg'; };
    } else {
        viewImg.src = '/Companys/Images/default.jpg';
    }
                    })
                    .catch(err => {
                        console.error("❌ VIEW ERROR:", err);
                        alert('Failed to load company details');
                    });
            });
        });

        // =================================================================
        // 🔥 DELETE BUTTON - FIXED (same SP aliases)
        // =================================================================
        document.querySelectorAll('.deleteCompanies').forEach(btn => {
            btn.addEventListener('click', function() {
                const id = this.dataset.companyId;
                document.getElementById('hdnDelCompanyId').value = id;
                fetch(`@Url.Action("GetCompany", "Company")?companyID=${id}`)
                    .then(r => r.json())
                    .then(data => {
                        console.log("🗑️ DELETE DATA:", data);

    document.getElementById('lblDelCompanyName').textContent    = data.companyName || 'N/A';
    document.getElementById('lblDelCompanyEmail').textContent   = data.email       || 'N/A';
    document.getElementById('lblDelCompanyAddress1').textContent= data.address1    || 'N/A';
    document.getElementById('lblDelCompanyCity').textContent    = data.city        || 'N/A';
    document.getElementById('lblDelCompanyState').textContent   = data.stateName   || 'N/A';
    document.getElementById('lblDelCompanyCountry').textContent = data.countryName || 'N/A';

                           const delImg = document.getElementById('image_upload_previewCompanyDel');
    const imagePath = data.picturePath;
    if (imagePath && imagePath.trim() !== '') {
        delImg.src = imagePath;
        delImg.onerror = () => { delImg.src = '/Companys/Images/default.jpg'; };
    } else {
        delImg.src = '/Companys/Images/default.jpg';
    }
                    })
                    .catch(err => {
                        console.error("❌ DELETE LOAD ERROR:", err);
                        alert('Failed to load delete details');
                    });
            });
        });

        // =================================================================
        // 🔥 EDIT SUBMIT
        // =================================================================
        document.querySelectorAll('.edit-submit').forEach(btn => {
            btn.addEventListener('click', async function(e) {
                e.preventDefault();
                let companyId = document.getElementById('hdnCompanyId')?.value;
                if (!companyId || companyId === '0') {
                    alert('⚠️ Please click EDIT button first');
                    return;
                }
                const formData = new FormData();
                const file = document.getElementById('editLogoUpload')?.files[0];
                if (file) formData.append('file', file);

                const companyData = {
                    CompanyID: parseInt(companyId),
                    Comp_Name: document.getElementById('txtEditCompanyName').value,
                    Comp_Address1: document.getElementById('txtEditCompanyAddress1').value,
                    Comp_Address2: document.getElementById('txtEditCompanyAddress2').value,
                    Comp_Area: document.getElementById('txtEditArea').value,
                    Comp_City: document.getElementById('txtEditCity').value,
                    Comp_State_ID: parseInt(document.getElementById('ddlEditStateId').value || 0),
                    Comp_Country_ID: parseInt(document.getElementById('ddlEditCountryId').value || 0),
                    Comp_Pincode: document.getElementById('txtEditPincode').value,
                    Comp_Contact_No: document.getElementById('txtEditContactNo').value,
                    Comp_Email: document.getElementById('txtEditEmail').value,
                    Comp_PAN_No: document.getElementById('txtEditPAN').value,
                    Comp_TAN_No: document.getElementById('txtEditTAN').value,
                    Comp_VAT_No: document.getElementById('txtEditVAT').value,
                    Comp_GST_No: document.getElementById('txtEditGST').value,
                    Comp_TIN_No: document.getElementById('txtEditTIN').value,
                    Comp_PTTax_No: document.getElementById('txtEditPT').value,
                    SMTP_Server_Name: document.getElementById('txtEditSMTPServerName').value,
                    SMTP_Port_No: document.getElementById('txtEditSMTPPortNo').value,
                    SMTP_User_Name: document.getElementById('txtEditSMTPUserName').value,
                    SMTP_User_Password: document.getElementById('txtEditSMTPPassword').value,
                    IsActive: document.getElementById('chkEditActiveStatus').checked ? 1 : 0
                };
                formData.append('companyJson', JSON.stringify(companyData));
                try {
                    const response = await fetch('@Url.Action("Edit", "Company")', { method: 'POST', body: formData });
                    const result = await response.json();
                    if (result.success !== false) {
                        alert('✅ Company Updated Successfully!');
                        $('#editCompany').modal('hide');
                        setTimeout(() => location.reload(), 1000);
                    } else {
                        alert('❌ ' + (result.message || 'Update failed'));
                    }
                } catch (error) {
                    alert('Network Error');
                }
            });
        });

        // =================================================================
        // 🔥 DELETE SUBMIT
        // =================================================================
        document.querySelectorAll('.delete-submit').forEach(btn => {
            btn.addEventListener('click', async function(e) {
                e.preventDefault();
                const id = document.getElementById('hdnDelCompanyId').value;
                if (!id) { alert('❌ No company selected'); return; }
                if (!confirm('⚠️ Are you SURE you want to PERMANENTLY DELETE this company?')) return;
                try {
                    const response = await fetch(`@Url.Action("DeleteCompany", "Company")?companyId=${id}`);
                    const result = await response.json();
                    if (result.success) {
                        alert('✅ ' + result.message);
                        $('#deleteCompany').modal('hide');
                        setTimeout(() => location.reload(), 800);
                    } else {
                        alert('❌ ' + result.message);
                    }
                } catch (error) {
                    alert('Network Error');
                }
            });
        });

        // =================================================================
        // 🔥 FILL EDIT FORM - FIXED (SP aliases mapped correctly)
         function fillEditForm(data) {
    const fields = {
        'txtEditCompanyName':     data.companyName    || '',
        'txtEditCompanyAddress1': data.address1       || '',
        'txtEditCompanyAddress2': data.address2       || '',
        'txtEditArea':            data.area           || '',
        'txtEditCity':            data.city           || '',
        'ddlEditStateId':         data.comp_State_ID  || '',
        'ddlEditCountryId':       data.comp_Country_ID|| '',
        'txtEditPincode':         data.pincode        || '',
        'txtEditContactNo':       data.contactNo      || '',
        'txtEditEmail':           data.email          || '',
        'txtEditPAN':             data.panNo          || '',
        'txtEditTAN':             data.tanNo          || '',
        'txtEditVAT':             data.vatNo          || '',
        'txtEditGST':             data.gstNo          || '',
        'txtEditTIN':             data.tinNo          || '',
        'txtEditPT':              data.ptTaxNo        || '',
        // 🔥 KEY FIX - exact camelCase from your JSON
        'txtEditSMTPServerName':  data.smtpServerName  || '',
        'txtEditSMTPPortNo':      data.smtpPortNo      || '',
        'txtEditSMTPUserName':    data.smtpUserName    || '',
        'txtEditSMTPPassword':    data.smtpUserPassword|| ''
    };

    Object.entries(fields).forEach(([id, value]) => {
        const el = document.getElementById(id);
        if (el) el.value = value || '';
    });

    const chkActive = document.getElementById('chkEditActiveStatus');
    if (chkActive) chkActive.checked = (data.isActive == 1);
}

        // =================================================================
        // 🔥 RESET BUTTONS
        // =================================================================
        document.querySelectorAll('.reset-btn').forEach(btn => {
            btn.addEventListener('click', function() {
                const form = this.closest('.modal').querySelector('form');
                if (form) form.reset();
                document.getElementById('logoPreview').src = '/Companys/Images/default.jpg';
                document.getElementById('editLogoPreview').src = '/Companys/Images/default.jpg';
            });
        });

        console.log("✅ ALL HANDLERS LOADED");
    });
</script>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        const table = document.getElementById("companyTable");
        if (!table) return;

        const rows = table.querySelectorAll("tbody tr");
        const pagination = document.getElementById("pagination");
        const showAllBtn = document.getElementById("showAllButton");

        let currentPage = 1;
        const pageSize = 5;
        const totalRows = rows.length;
        const totalPages = Math.ceil(totalRows / pageSize);

        function showPage(page) {
            currentPage = page;
            rows.forEach((row, index) => {
                const start = (page - 1) * pageSize;
                const end = start + pageSize;
                row.style.display = (index >= start && index < end) ? "" : "none";
            });
            renderPagination();
        }

        function renderPagination() {
            if (!pagination) return;
            pagination.innerHTML = "";

            const prev = document.createElement("li");
            prev.className = "page-item " + (currentPage === 1 ? "disabled" : "");
            prev.innerHTML = '<a class="page-link" href="#">Previous</a>';
            prev.onclick = function(e) { e.preventDefault(); if (currentPage > 1) showPage(currentPage - 1); };
            pagination.appendChild(prev);

            for (let i = 1; i <= totalPages; i++) {
                const li = document.createElement("li");
                li.className = "page-item " + (i === currentPage ? "active" : "");
                li.innerHTML = '<a class="page-link" href="#">' + i + '</a>';
                li.onclick = function(e) { e.preventDefault(); showPage(i); };
                pagination.appendChild(li);
            }

            const next = document.createElement("li");
            next.className = "page-item " + (currentPage === totalPages ? "disabled" : "");
            next.innerHTML = '<a class="page-link" href="#">Next</a>';
            next.onclick = function(e) { e.preventDefault(); if (currentPage < totalPages) showPage(currentPage + 1); };
            pagination.appendChild(next);
        }

        if (showAllBtn) {
            showAllBtn.addEventListener("click", function() {
                rows.forEach(row => row.style.display = "");
                if (pagination) pagination.innerHTML = "";
                this.style.display = "none";
            });
        }

        if (rows.length > 0) showPage(1);
    });
</script>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        console.log("✅ Validation Script Loaded");

        function showError(field, message) {
            if (!field) return;
            field.style.border = "1px solid red";
            let msg = field.parentElement.nextElementSibling;
            if (msg) msg.textContent = message;
        }

        function clearError(field) {
            if (!field) return;
            field.style.border = "1px solid #ddd";
            let msg = field.parentElement.nextElementSibling;
            if (msg) msg.textContent = "";
        }

        function validateEmail(email) { return /^\S+@@\S+\.\S+$/.test(email); }
        function validatePhone(phone) { return /^[0-9]{10}$/.test(phone); }
        function validatePin(pin)     { return /^[0-9]{6}$/.test(pin); }
        function validatePAN(pan)     { return /^[A-Z]{5}[0-9]{4}[A-Z]{1}$/.test(pan); }

        function validateAddForm() {
            let isValid = true;
            const fields = {
                txtCompanyName: "Company Name", txtCompanyAddress1: "Address",
                txtCity: "City", ddlStateId: "State", ddlCountryId: "Country",
                txtPincode: "PIN", txtEmail: "Email", txtContactNo: "Contact"
            };
            Object.keys(fields).forEach(id => {
                const field = document.getElementById(id);
                if (!field || !field.value.trim()) { showError(field, fields[id] + " is required"); isValid = false; }
                else clearError(field);
            });
            const email = document.getElementById("txtEmail");
            if (email.value && !validateEmail(email.value)) { showError(email, "Invalid email"); isValid = false; }
            const phone = document.getElementById("txtContactNo");
            if (phone.value && !validatePhone(phone.value)) { showError(phone, "10 digit number required"); isValid = false; }
            const pin = document.getElementById("txtPincode");
            if (pin.value && !validatePin(pin.value)) { showError(pin, "Invalid PIN"); isValid = false; }
            const pan = document.getElementById("txtPAN");
            if (pan.value && !validatePAN(pan.value)) { showError(pan, "Invalid PAN"); isValid = false; }
            return isValid;
        }

        function validateEditForm() {
            let isValid = true;
            const fields = {
                txtEditCompanyName: "Company Name", txtEditCompanyAddress1: "Address",
                txtEditCity: "City", ddlEditStateId: "State", ddlEditCountryId: "Country",
                txtEditPincode: "PIN", txtEditEmail: "Email", txtEditContactNo: "Contact"
            };
            Object.keys(fields).forEach(id => {
                const field = document.getElementById(id);
                if (!field || !field.value.trim()) { showError(field, fields[id] + " is required"); isValid = false; }
                else clearError(field);
            });
            const email = document.getElementById("txtEditEmail");
            if (email.value && !validateEmail(email.value)) { showError(email, "Invalid email"); isValid = false; }
            const phone = document.getElementById("txtEditContactNo");
            if (phone.value && !validatePhone(phone.value)) { showError(phone, "Invalid number"); isValid = false; }
            const pin = document.getElementById("txtEditPincode");
            if (pin.value && !validatePin(pin.value)) { showError(pin, "Invalid PIN"); isValid = false; }
            return isValid;
        }

        document.querySelectorAll(".add-submit").forEach(btn => {
            btn.addEventListener("click", function(e) {
                if (!validateAddForm()) { e.stopImmediatePropagation(); }
            }, true);
        });

        document.querySelectorAll(".edit-submit").forEach(btn => {
            btn.addEventListener("click", function(e) {
                if (!validateEditForm()) { e.stopImmediatePropagation(); }
            }, true);
        });
    });
</script>

